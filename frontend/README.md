Frontend (React + Vite)

Install and run:

```
cd frontend
npm install
npm run dev
```

Pages:
- Landing: `/` (src/pages/Landing.jsx)
- Login: `/login` (stub)
- Signup: `/signup` (stub)

Firebase client config is in `src/firebase.js`.
